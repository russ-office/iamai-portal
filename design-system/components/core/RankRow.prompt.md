One leaderboard row (`c-rank-row`): position, avatar, name, status dot, right-aligned mono number. Stack inside a `c-sheet`.

```jsx
<RankRow position={1} name="Аружан Сериккызы" status="ok" value="18.5" />
```
