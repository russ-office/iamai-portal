A KPI number on its own D1 sheet (`c-kpi-tile`). The label and value ellipsis so the tile sizes to the row, never to the widest number. Place several in a flex row.

```jsx
<KpiTile label="Потенциал" value="31" sub="часов / мес" />
```
