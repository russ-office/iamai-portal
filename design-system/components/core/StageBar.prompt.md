Thread / pipeline progress bar (`c-stage-bar`). Terracotta fill while in progress, sage when complete. Meta is mono + tabular.

```jsx
<StageBar label="Прототип" value={1} meta="6.5 ч · 100%" />
<StageBar label="One-pager" value={0.1} meta="4 ч · 10%" />
```
