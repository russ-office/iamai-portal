A labelled number (`c-metric`). The value is always JetBrains Mono with tabular figures.

```jsx
<Metric value="18.5" label="часов / мес" />
<Metric value="34%" label="доля вклада" sub="high-impact" />
```
