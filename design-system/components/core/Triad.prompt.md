The Триада: Problem / Output / Input in three hairline-separated columns — the structural core of a task (`c-triad`). No nested cards.

```jsx
<Triad problem="Ручной сбор интервью" output="Единый формат в базе" input="20 интервью, гайд" />
```
